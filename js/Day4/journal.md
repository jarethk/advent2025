Part 1 - this seems like a simple count of neighbors, so check through the grid and return the rolls that match to get the count.

Part 2 - Now after we get what can be removed, then remove them and repeat the process until there are none that can be removed.
